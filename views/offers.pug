extends base

block content
  section.page-header
    .container
      h1 العروض 🎉
      p عرض مختلف كل يوم من أيام الأسبوع – استمتع بأشهى الأطباق بأسعار مميزة!

  section.products-section.offers-section
    .container
      #offersGrid.products-grid
        if offersList && offersList.length
          - var i = 0
          while i < offersList.length
            - var currentOffer = offersList[i++]
            .product-card(data-category=currentOffer.category)
              .product-image
                img(src=currentOffer.image, alt=currentOffer.name)
              .product-info
                h3= currentOffer.name
                p= currentOffer.description
              .product-footer
                .product-price
                  span.old-price= currentOffer.price
                  span.price= currentOffer.newprice
                  span.currency جنيه
                button.btn.btn-add-cart(data-name=currentOffer.name data-price=currentOffer.newprice data-image=currentOffer.image) أضف للسلة
        else
          p.no-offers لا توجد عروض متاحة حالياً
      .section-cta
        a.btn.btn-outline(href="/review")     ادخل قيم المطعم
