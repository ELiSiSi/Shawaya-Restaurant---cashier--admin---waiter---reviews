extends base

block content
  // Loading Spinner
  #loadingSpinner.loading-spinner
    .spinner

  // Page Header
  section.page-header
    .container
      h1 🛒 سلة التسوق
      p راجع طلبك قبل الإتمام 

  // Cart Section
  section.cart-section
    .container
      .cart-wrapper

        // Empty Cart
        #emptyCart.empty-cart
          .empty-cart-icon 🛒
          h2 السلة فارغة
          p لم تقم بإضافة أي منتجات بعد
          a.btn.btn-primary(href="/menu") تصفح المنتجات

        // Cart Content
        #cartContent.cart-content(style="display:none")
          form#cartForm

            // Cart Items
            #cartItems.cart-items
              // يتم الإضافة بالـ JS

            // Cart Summary
            .cart-summary
              .summary-row
                span المجموع الفرعي:
                span#subtotal 0 جنيه

              .summary-divider

              .summary-row.total
                span المجموع الإجمالي:
                span#total 0 جنيه

              // Customer Info
              .customer-info(style="margin-top:25px")
                h4(style="color: var(--primary-color); margin-bottom:15px") بيانات التوصيل

                .form-group(style="margin-bottom:15px")
                  label(for="customerName")
                    | الاسم الكامل
                    span(style="color:#ff4444") *
                  input#customerName(
                    type="text"
                    name="customerName"
                    placeholder="محمد أحمد"
                    required
                  )

                .form-group(style="margin-bottom:15px")
                  label(for="customerPhone")
                    | رقم التليفون
                    span(style="color:#ff4444") *
                  input#customerPhone(
                    type="tel"
                    name="customerPhone"
                    placeholder="01012345678"
                    required
                  )

                .form-group(style="margin-bottom:15px")
                  label(for="customerAddress")
                    | العنوان بالتفصيل
                    span(style="color:#ff4444") *
                  textarea#customerAddress(
                    name="customerAddress"
                    rows="3"
                    placeholder="مثال: شارع النيل، المعادي، الدور الثالث، شقة 5"
                    required
                  )

                .form-group(style="margin-bottom:20px")
                  label(for="notes") ملاحظات إضافية (اختياري)
                  textarea#notes(name="notes" rows="2" placeholder="أي ملاحظات خاصة بالطلب...")

              button.btn.btn-primary.btn-checkout(type="submit") إتمام الطلب
              a.btn.btn-outline(href="/menu") متابعة التسوق

  // Success Modal
  .modal#successModal
    .modal-content
      .modal-icon.success ✓
      h2 تم الطلب بنجاح!
      p شكراً لك! سيتم التواصل معك قريباً لتأكيد الطلب.
      button.btn.btn-primary(onclick="closeModal()") حسناً
